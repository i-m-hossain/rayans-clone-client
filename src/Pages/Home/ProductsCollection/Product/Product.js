import React from 'react';

const Product = ({ product }) => {
    const { image, title, short_des, rating, price } = product;
    return (
        <div>

        </div>
    );
};

export default Product;